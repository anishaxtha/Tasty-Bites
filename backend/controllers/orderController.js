import Stripe from "Stripe";
import userModel from "../models/userModel";
import orderModel from "../models/orderModel";

// placing the order model for frontend

const placeOrder = async (req, res) => {};

export { placeOrder };
